# GIT PROMPT
if [ -f ~/.git-prompt ]; then
    source ~/.git-prompt
fi

# BASH THEME
if [ -f ~/.bash_theme ]; then
    source ~/.bash_theme
fi

# BASH ENV
if [ -f ~/.bash_env ]; then
    source ~/.bash_env
fi

# BASH ALIASES
if [ -f ~/.bash_aliases ]; then
    source ~/.bash_aliases
fi


